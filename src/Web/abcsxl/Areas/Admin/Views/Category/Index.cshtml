@model IEnumerable<abcsxl.Models.Entities.Category>

@{
    ViewData["Title"] = T["PageTitle"].Value;
    var currentPage = ViewBag.CurrentPage ?? 1;
    var totalPages = ViewBag.TotalPages ?? 1;
    var search = ViewBag.Search as string;
}

<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0"><i class="bi bi-folder"></i> @T["HeaderTitle"]</h5>
        <a asp-action="Create" class="btn btn-primary btn-sm">
            <i class="bi bi-plus-lg"></i> @T["CreateButton"]
        </a>
    </div>
    <div class="card-body">
        @if (TempData["Success"] != null)
        {
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                @TempData["Success"]
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        }

        <form asp-action="Index" method="get" class="mb-3">
            <div class="row g-2">
                <div class="col-md-4">
                    <input type="text" name="search" class="form-control" placeholder="@T["SearchPlaceholder"]" value="@search">
                </div>
                <div class="col-md-2">
                    <button type="submit" class="btn btn-outline-primary w-100">
                        <i class="bi bi-search"></i> @T["SearchButton"]
                    </button>
                </div>
                @if (!string.IsNullOrEmpty(search))
                {
                    <div class="col-md-2">
                        <a asp-action="Index" class="btn btn-outline-secondary w-100">
                            <i class="bi bi-x-circle"></i> @T["ResetButton"]
                        </a>
                    </div>
                }
            </div>
        </form>

        @if (Model.Any())
        {
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead class="table-light">
                        <tr>
                            <th>@T["TableHeaderName"]</th>
                            <th>@T["TableHeaderDescription"]</th>
                            <th>@T["TableHeaderParent"]</th>
                            <th>@T["TableHeaderOrder"]</th>
                            <th>@T["TableHeaderPosts"]</th>
                            <th style="width: 120px;">@T["TableHeaderActions"]</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var category in Model)
                        {
                            <tr>
                                <td><strong>@category.Name</strong></td>
                                <td><small class="text-muted">@category.Description</small></td>
                                <td>
                                    @if (category.Parent != null)
                                    {
                                        <span class="badge bg-secondary">@category.Parent.Name</span>
                                    }
                                    else
                                    {
                                        <span class="text-muted">—</span>
                                    }
                                </td>
                                <td>@category.Order</td>
                                <td>@category.Posts.Count</td>
                                <td>
                                    <div class="btn-group btn-group-sm">
                                        <a asp-action="Edit" asp-route-id="@category.Id" class="btn btn-outline-primary" title="@T["ActionEdit"]">
                                            <i class="bi bi-pencil"></i>
                                        </a>
                                        <a asp-action="Delete" asp-route-id="@category.Id" class="btn btn-outline-danger" title="@T["ActionDelete"]" onclick="return confirm('@T["ConfirmDelete"]')">
                                            <i class="bi bi-trash"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

            @if (totalPages > 1)
            {
                <nav aria-label="@T["PaginationAriaLabel"]">
                    <ul class="pagination justify-content-center">
                        <li class="page-item @(currentPage <= 1 ? "disabled" : "")">
                            <a class="page-link" asp-action="Index" asp-route-page="@(currentPage - 1)" asp-route-search="@search">
                                <i class="bi bi-chevron-left"></i>
                            </a>
                        </li>

                        @for (var i = 1; i <= totalPages; i++)
                        {
                            @if (i == 1 || i == totalPages || (i >= currentPage - 2 && i <= currentPage + 2))
                            {
                                <li class="page-item @(i == currentPage ? "active" : "")">
                                    <a class="page-link" asp-action="Index" asp-route-page="@i" asp-route-search="@search">@i</a>
                                </li>
                            }
                            else if (i == currentPage - 3 || i == currentPage + 3)
                            {
                                <li class="page-item disabled"><span class="page-link">...</span></li>
                            }
                        }

                        <li class="page-item @(currentPage >= totalPages ? "disabled" : "")">
                            <a class="page-link" asp-action="Index" asp-route-page="@(currentPage + 1)" asp-route-search="@search">
                                <i class="bi bi-chevron-right"></i>
                            </a>
                        </li>
                    </ul>
                </nav>
            }
        }
        else
        {
            <div class="text-center py-5">
                <p class="text-muted">@T["NoCategories"]</p>
                <a asp-action="Create" class="btn btn-primary">@T["CreateFirstCategory"]</a>
            </div>
        }
    </div>
</div>
