@model IEnumerable<abcsxl.Models.Entities.Post>
@using abcsxl.Models.Enums

@{
    ViewData["Title"] = "文章管理";
    var currentPage = ViewBag.CurrentPage ?? 1;
    var totalPages = ViewBag.TotalPages ?? 1;
    var search = ViewBag.Search as string;
    var status = ViewBag.Status as string;
}

<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0">📝 文章管理</h5>
        <a asp-area="Admin" asp-controller="Post" asp-action="Create" class="btn btn-primary btn-sm">
            <i class="bi bi-plus-circle"></i> 新建文章
        </a>
    </div>
    <div class="card-body">
        <!-- 搜索和筛选 -->
        <form asp-action="Index" method="get" class="mb-3">
            <div class="row g-2">
                <div class="col-md-4">
                    <input type="text" name="search" class="form-control" placeholder="搜索标题或内容..." value="@search">
                </div>
                <div class="col-md-3">
                    <select name="status" class="form-select">
                        <option value="">全部状态</option>
                        <option value="Draft" selected="@(status == "Draft")">草稿</option>
                        <option value="Published" selected="@(status == "Published")">已发布</option>
                    </select>
                </div>
                <div class="col-md-2">
                    <button type="submit" class="btn btn-outline-primary w-100">
                        <i class="bi bi-search"></i> 搜索
                    </button>
                </div>
                @if (!string.IsNullOrEmpty(search) || !string.IsNullOrEmpty(status))
                {
                    <div class="col-md-2">
                        <a asp-action="Index" class="btn btn-outline-secondary w-100">
                            <i class="bi bi-x-circle"></i> 重置
                        </a>
                    </div>
                }
            </div>
        </form>

        @if (Model.Any())
        {
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>标题</th>
                        <th>作者</th>
                        <th>浏览</th>
                        <th>发布时间</th>
                        <th>状态</th>
                        <th style="width: 120px;">操作</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var post in Model)
                    {
                        <tr>
                            <td>
                                <a asp-area="" asp-controller="Post" asp-action="Detail" asp-route-id="@post.Slug" target="_blank">
                                    @post.Title
                                </a>
                            </td>
                            <td>@post.Author.Username</td>
                            <td><span class="text-muted">@post.ViewCount</span></td>
                            <td>
                                @(post.PublishedAt?.ToString("yyyy-MM-dd") ?? "—")
                            </td>
                            <td>
                                @if (post.Status == PostStatus.Published)
                                {
                                    <span class="badge bg-success">已发布</span>
                                }
                                else
                                {
                                    <span class="badge bg-secondary">草稿</span>
                                }
                            </td>
                            <td>
                                <div class="btn-group btn-group-sm">
                                    <a asp-area="Admin" asp-controller="Post" asp-action="Edit" asp-route-id="@post.Id" class="btn btn-outline-primary" title="编辑">
                                        <i class="bi bi-pencil"></i>
                                    </a>
                                    <a asp-area="" asp-controller="Post" asp-action="Detail" asp-route-id="@post.Slug" class="btn btn-outline-secondary" target="_blank" title="查看">
                                        <i class="bi bi-eye"></i>
                                    </a>
                                    <a asp-area="Admin" asp-controller="Post" asp-action="Delete" asp-route-id="@post.Id" class="btn btn-outline-danger" title="删除">
                                        <i class="bi bi-trash"></i>
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>

            <!-- 分页 -->
            @if (totalPages > 1)
            {
                <nav aria-label="文章分页">
                    <ul class="pagination justify-content-center">
                        <li class="page-item @(currentPage <= 1 ? "disabled" : "")">
                            <a class="page-link" asp-action="Index" asp-route-page="@(currentPage - 1)" asp-route-search="@search" asp-route-status="@status">
                                <i class="bi bi-chevron-left"></i>
                            </a>
                        </li>

                        @for (var i = 1; i <= totalPages; i++)
                        {
                            @if (i == 1 || i == totalPages || (i >= currentPage - 2 && i <= currentPage + 2))
                            {
                                <li class="page-item @(i == currentPage ? "active" : "")">
                                    <a class="page-link" asp-action="Index" asp-route-page="@i" asp-route-search="@search" asp-route-status="@status">@i</a>
                                </li>
                            }
                            else if (i == currentPage - 3 || i == currentPage + 3)
                            {
                                <li class="page-item disabled"><span class="page-link">...</span></li>
                            }
                        }

                        <li class="page-item @(currentPage >= totalPages ? "disabled" : "")">
                            <a class="page-link" asp-action="Index" asp-route-page="@(currentPage + 1)" asp-route-search="@search" asp-route-status="@status">
                                <i class="bi bi-chevron-right"></i>
                            </a>
                        </li>
                    </ul>
                </nav>
            }
        }
        else
        {
            <div class="text-center py-5">
                <p class="text-muted">暂无文章。</p>
            </div>
        }
    </div>
</div>