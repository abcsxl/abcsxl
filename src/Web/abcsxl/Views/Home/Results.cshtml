@model abcsxl.Models.ViewModels.Home.SearchResultViewModel
@{
    ViewData["Title"] = !string.IsNullOrEmpty(Model.Query) ? $"搜索结果 - {Model.Query}" : "搜索";
}

@section Styles
{
    <link rel="stylesheet" href="~/css/page-specific-results.css" />
}

<!-- Page Header-->
<header class="masthead" style="background-image: url('../images/results-bg.webp')">
    <div class="container position-relative px-4 px-lg-5">
        <div class="row gx-4 gx-lg-5 justify-content-center">
            <div class="col-md-10 col-lg-9 col-xl-8">
                <div class="site-heading">
                    <h1>@ViewData["Title"]</h1>
                    <span class="subheading">
                        以无生之觉悟为有生之事业，以悲观之心情过乐观之生活。
                    </span>
                </div>
            </div>
        </div>
    </div>
</header>

<!-- Main Content-->
<div class="container px-4 px-lg-5">
    <div class="row gx-4 gx-lg-5 justify-content-center">
        <div class="col-md-10 col-lg-9 col-xl-8">

            <!-- 搜索框 -->
            <div class="search-section">
                <div class="container">
                    <form method="get" class="search-box-container">
                        <input type="text" name="q" class="search-box" placeholder="输入搜索关键词..." value="@Model.Query">
                        <input type="hidden" name="tab" value="@Model.ActiveTab" />
                        <button type="submit" class="search-button">
                            <i class="fas fa-search"></i>
                        </button>
                    </form>
                </div>
            </div>

            @if (!string.IsNullOrEmpty(Model.Query))
            {
                <!-- 结果统计和筛选 -->
                <div class="results-header">
                    <div class="results-count">共找到 @Model.TotalCount 条关于 "@Model.Query" 的结果</div>
                    <div class="results-tabs" id="resultsTabs">
                        <a href="@Url.Action("Results", "Home", new { q = Model.Query, tab = "all" })" class="tab @(Model.ActiveTab == "all" ? "active" : "")">全部</a>
                        <a href="@Url.Action("Results", "Home", new { q = Model.Query, tab = "post" })" class="tab @(Model.ActiveTab == "post" ? "active" : "")">文章</a>
                        <a href="@Url.Action("Results", "Home", new { q = Model.Query, tab = "category" })" class="tab @(Model.ActiveTab == "category" ? "active" : "")">分类</a>
                        <a href="@Url.Action("Results", "Home", new { q = Model.Query, tab = "tag" })" class="tab @(Model.ActiveTab == "tag" ? "active" : "")">标签</a>
                    </div>
                </div>

                <!-- 结果列表 -->
                <div class="results-container" id="resultsContainer">
                    @if (Model.ActiveTab == "all" || Model.ActiveTab == "post")
                    {
                        @foreach (var post in Model.Posts)
                        {
                            <div class="result-item">
                                <span class="result-category">文章</span>
                                <a href="@post.Url" class="result-title">@post.Title</a>
                                <p class="result-description">@post.Excerpt</p>
                                <small class="text-muted">@post.Author · @post.PublishedAt.ToString("yyyy-MM-dd")</small>
                            </div>
                        }
                    }

                    @if (Model.ActiveTab == "all" || Model.ActiveTab == "category")
                    {
                        @foreach (var category in Model.Categories)
                        {
                            <div class="result-item">
                                <span class="result-category">分类</span>
                                <a href="@category.Url" class="result-title">@category.Name</a>
                                <small class="text-muted">@category.PostCount 篇文章</small>
                            </div>
                        }
                    }

                    @if (Model.ActiveTab == "all" || Model.ActiveTab == "tag")
                    {
                        @foreach (var tag in Model.Tags)
                        {
                            <div class="result-item">
                                <span class="result-category">标签</span>
                                <a href="@tag.Url" class="result-title">@tag.Name</a>
                                <small class="text-muted">@tag.PostCount 篇文章</small>
                            </div>
                        }
                    }

                    @if (Model.TotalCount == 0)
                    {
                        <div class="text-center py-5">
                            <p class="text-muted">未找到相关内容，请尝试其他关键词。</p>
                        </div>
                    }
                </div>
            }
            else
            {
                <div class="text-center py-5">
                    <p class="text-muted">请输入搜索关键词。</p>
                </div>
            }

        </div>
    </div>
</div>